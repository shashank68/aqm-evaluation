diff --git a/flent/tests/tcp_nup.conf b/flent/tests/tcp_nup.conf
index be278e3..9393194 100644
--- a/flent/tests/tcp_nup.conf
+++ b/flent/tests/tcp_nup.conf
@@ -18,6 +18,12 @@ DATA_SETS = o([
     ('Ping (ms) ICMP',
      {'ip_version': IP_VERSION, 'interval': STEP_SIZE, 'length': TOTAL_LENGTH,
       'host': HOST, 'units': 'ms', 'runner': 'ping'}),
+    ('Link Utilization',
+      {'interval': STEP_SIZE, 'length': TOTAL_LENGTH, 'units': 'ms','host': HOST, 'runner': 'ping'}),
+    ('TCP Upload: Cumulative',
+     {'apply_to': [glob("TCP upload::*", exclude=["TCP upload sum"])],
+      'units': 'Mbits/s',
+      'runner': 'cumulative',}),
 ])
 
 def add_stream(i, marking='CS0', cc_algo=None,
@@ -55,7 +61,12 @@ PLOTS = o([
                   'label': 'Avg',
                   'smoothing': 10,
                   'color': 'black',
-                  'linewidth': 2}]}),
+                  'linewidth': 2},
+                  {
+                    'data': 'TCP Upload: Cumulative',
+                    'label': 'Cumulative',
+                  },
+                ]}),
     ('upload_box',
      {'description': 'Upload box plot',
       'type': 'box',
